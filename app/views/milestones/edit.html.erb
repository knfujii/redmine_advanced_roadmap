<h2><%= label_milestone %></h2>

<%= labelled_form_for(@milestone) do |f| %>
  <%= render(:partial => 'form', :locals => {:f => f}) %>
  <%= submit_tag(l(:button_save)) %>
<% end %>

<%= render :partial => 'common/advanced_roadmap_footer' %>
