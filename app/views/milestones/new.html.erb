<h2><%= l(:label_milestone_new, :milestone_label => label_milestone) %></h2>

<%= labelled_form_for([@project, @milestone]) do |f| %>
  <%= render(:partial => 'form', :locals => {:f => f}) %>
  <%= submit_tag(l(:button_create)) %>
<% end %>

<%= render :partial => 'common/advanced_roadmap_footer' %>
